# Schema Inventory

Generated from the latest normalized Monday export.

## Current Monday Architecture

- Account: {{ model.account.get('name', 'Unknown') }}
- Users exported: {{ model.users | length }}
- Workspaces: {{ model.workspaces | length }}
- Boards: {{ model.boards | length }}

## Workspace Inventory

| Workspace | ID | Boards |
| --- | --- | ---: |
{% for workspace in model.workspaces -%}
| {{ workspace.name }} | {{ workspace.id }} | {{ workspace.board_ids | length }} |
{% endfor %}

## Board Inventory

| Board | ID | Workspace | Groups | Columns | Sample Items |
| --- | --- | --- | ---: | ---: | ---: |
{% for board in model.boards -%}
| {{ board.name }} | {{ board.id }} | {{ board.workspace_id or 'None' }} | {{ board.groups | length }} | {{ board.columns | length }} | {{ board.item_count if board.item_count is not none else 'n/a' }} |
{% endfor %}

## Column Inventory

{% for board in model.boards %}
### {{ board.name }} (`{{ board.id }}`)

| Column | Type | Normalized Type | Labels | Relationship Targets |
| --- | --- | --- | --- | --- |
{% for column in board.columns -%}
| {{ column.title }} | {{ column.type }} | {{ column.normalized_type }} | {{ column.labels | join(', ') }} | {{ column.relationship_targets | join(', ') }} |
{% endfor %}
{% endfor %}
